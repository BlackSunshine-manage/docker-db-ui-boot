package com.company.project.utils.api;

import static com.company.project.utils.api.ApiStringUtils.*;

public interface ApiStringConstants {
    String API_V1 = BACK_SLASH.getValue() + API + BACK_SLASH + V1 + "";
}
